console.log(0);
function setup(){
  createCanvas(600,600);
  defaultCanvas0.style.position = 'absolute';
  defaultCanvas0.style.top = (window.innerHeight-600)/2+'px';
  defaultCanvas0.style.left = (window.innerWidth-600)/2+'px';
}
function draw(){
  background(250);
}
